baupro/
├── src/
│   ├── App.jsx              # Die komplette App (1539 Zeilen)
│   └── main.jsx             # React Entry Point
├── public/                  # Statische Dateien (leer = keine nötig)
├── .github/
│   └── workflows/
│       └── deploy.yml       # GitHub Actions Auto-Deploy
├── index.html               # HTML Template
├── package.json             # Dependencies (React, React-DOM)
├── vite.config.js           # Vite Build Config
├── .gitignore               # Git ignore Datei
├── README.md                # Projekt Info
└── SETUP.md                 # Diese Datei

---

WAS WIRD GEBAUT:

npm run build erstellt:
dist/
├── index.html
├── assets/
│   ├── App-xxxxx.js (minified)
│   └── App-xxxxx.css (minified)
└── ... (weitere Dateien)

Das wird zu Cloudflare Pages deployed!

---

GRÖSSEN:

src/App.jsx:     113 KB (React Code)
npm modules:     200+ MB (installiert lokal, nicht in GitHub!)
dist/ (gebaut):  ~150-200 KB (nur das wird deployed!)

---

WICHTIG:

.gitignore enthält:
- node_modules/ (wird nicht in GitHub gepusht)
- dist/ (wird von Cloudflare gebaut)
- Nur source code geht in GitHub!

